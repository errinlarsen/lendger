require "rspec"

# require any spec support files or shared behavior
Dir[File.expand_path("../shared/**/*.rb", __FILE__)].each { |f| require f }

RSpec.configure do |config|
  ## Nothing here (yet?)
end
